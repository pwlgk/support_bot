# Telegram Бот Службы Поддержки

Этот Telegram-бот предназначен для автоматизации процесса приема, обработки и отслеживания заявок на ремонт и техническую поддержку внутри организации. Он обеспечивает взаимодействие между клиентами, инженерами и администраторами системы.

## Основные возможности

### Общие функции
*   **`/start`**: Инициализация бота, регистрация/приветствие пользователя, отображение главного меню в зависимости от роли.
*   **`/help`**: Отображение справочной информации о доступных командах и функциях для роли пользователя.

### Функции для Клиента (`Client`)
*   **Создание заявки (`📝 Создать заявку` / `/new_request`)**:
    *   Пошаговый ввод данных через FSM: ФИО, Корпус, Кабинет, Описание проблемы, ПК/Инв. номер (опционально), Телефон.
    *   Возможность отмены (`❌ Отмена`) на любом шаге.
    *   Сохранение заявки со статусом `WAITING`.
    *   Получение подтверждения с номером заявки и уведомление инженеров.
*   **Просмотр своих заявок (`📄 Мои заявки` / `/my_requests`)**:
    *   Отображение списка текущих заявок (статусы `WAITING`, `IN_PROGRESS`).
    *   Показывает ID, дату, статус, инженера (если назначен), начало описания.

### Функции для Инженера (`Engineer`)
*   **Просмотр новых заявок (`👀 Новые заявки` / `/view_new_requests` или Инлайн-кнопка)**:
    *   Список заявок со статусом `WAITING`.
    *   Инлайн-кнопки для просмотра деталей каждой заявки.
*   **Просмотр деталей новой заявки**:
    *   Отображение полной информации по заявке.
    *   Инлайн-кнопка `✅ Принять в работу`.
*   **Принятие заявки**:
    *   Смена статуса на `IN_PROGRESS`.
    *   Назначение себя исполнителем.
    *   Уведомление клиента.
*   **Просмотр своих заявок в работе (`🛠️ Мои заявки в работе` / `/my_requests` или Инлайн-кнопка)**:
    *   Список заявок со статусом `IN_PROGRESS`, назначенных на данного инженера, с пагинацией.
    *   Инлайн-кнопки для просмотра деталей.
*   **Просмотр деталей своей заявки**:
    *   Отображение полной информации по заявке.
    *   Инлайн-кнопка `🏁 Завершить (Выполнено)`.
*   **Завершение/Архивация заявки**:
    *   Смена статуса на `ARCHIVED`.
    *   Фиксация времени выполнения и архивации.
    *   Уведомление клиента.
*   **Просмотр истории (`📚 История выполненных` / `/archive` или Инлайн-кнопка)**:
    *   Список выполненных (`ARCHIVED`) заявок, где исполнителем был *данный* инженер, с пагинацией.
    *   Просмотр деталей архивной заявки.
*   **Уведомления**: Получение уведомлений о каждой новой созданной заявке.

### Функции для Администратора (`Admin`)
*   **Доступ к панели (`👑 Панель администратора` / `/admin`)**:
    *   Отображение инлайн-меню с административными функциями.
*   **Управление пользователями (через инлайн-меню)**:
    *   Просмотр списка всех пользователей с пагинацией (роль, имя, ID).
    *   Просмотр профиля пользователя.
    *   Назначение ролей (Admin, Engineer, Client) через инлайн-кнопки.
*   **Просмотр активных заявок (через инлайн-меню)**:
    *   Список *всех* заявок в статусе `IN_PROGRESS` с пагинацией.
    *   Просмотр деталей заявки.
*   **Просмотр истории выполненных (через инлайн-меню)**:
    *   Список *всех* заявок в статусе `ARCHIVED` с пагинацией.
    *   Просмотр деталей заявки.

## Технологический стек

*   **Язык:** Python 3.11+
*   **Фреймворк для Telegram:** Aiogram 3.x
*   **База данных:** PostgreSQL 15+
*   **ORM/DB Driver:** SQLAlchemy 2.x (AsyncIO) + asyncpg
*   **Конфигурация:** python-dotenv
*   **Контейнеризация:** Docker, Docker Compose

## Подготовка к запуску

### Требования
*   Установленный [Docker](https://docs.docker.com/engine/install/)
*   Установленный [Docker Compose](https://docs.docker.com/compose/install/) (обычно идет вместе с Docker Desktop или устанавливается отдельно)
*   Редактор кода (например, VS Code)
*   Токен Telegram-бота (получается у @BotFather)

### Начальная настройка
1.  **Клонировать репозиторий:**
    ```bash
    git clone https://github.com/pwlgk/support_bot.git # Замените на ваш URL, если нужно
    cd support_bot
    ```
2.  **Настроить файл `.env`:**
    *   Создайте файл `.env` в корневой директории проекта (можно скопировать из `.env.example`, если он есть).
    *   Заполните файл **обязательными** значениями:
        ```dotenv
        # .env

        # --- Telegram Bot ---
        BOT_TOKEN=123456:ABC-DEF1234ghIkl-zyx57W2v1u123ew11 # Замените на ваш реальный токен!

        # --- PostgreSQL Database (для Docker Compose) ---
        POSTGRES_USER=support_user          # Имя пользователя БД (можно оставить)
        POSTGRES_PASSWORD=your_very_secure_password # Придумайте НАДЕЖНЫЙ пароль!
        POSTGRES_DB=support_bot_db          # Имя базы данных (можно оставить)
        DATABASE_HOST=db                    # Имя сервиса БД в docker-compose.yml (НЕ МЕНЯТЬ!)
        DATABASE_PORT=5432                  # Стандартный порт PostgreSQL
        ```
    *   **ВАЖНО:** Придумайте и запомните надежный `POSTGRES_PASSWORD`.
    *   **ВАЖНО:** Добавьте `.env` в ваш файл `.gitignore`, чтобы случайно не загрузить секреты в репозиторий.

## Запуск с помощью Docker Compose (Рекомендуемый способ)

Этот метод запускает и бота, и базу данных PostgreSQL в изолированных контейнерах.

1.  **Сборка и запуск контейнеров:**
    *   Откройте терминал в корневой папке проекта (где находится `docker-compose.yml`).
    *   Выполните команду:
        ```bash
        docker compose up --build -d
        ```
        *   `--build`: Пересобирает образ бота, если вы вносили изменения в код или `Dockerfile`.
        *   `-d`: Запускает контейнеры в фоновом (detached) режиме.
    *   При первом запуске Docker загрузит образ PostgreSQL и соберет образ для вашего бота. База данных будет инициализирована с учетными данными из `.env`.

2.  **Проверка статуса:**
    ```bash
    docker compose ps
    ```
    Вы должны увидеть два работающих контейнера: `support_postgres_db` и `support_telegram_bot`.

3.  **Просмотр логов:**
    *   Логи бота: `docker compose logs -f bot`
    *   Логи базы данных: `docker compose logs -f db`
    *   Нажмите `Ctrl+C`, чтобы остановить просмотр логов.

4.  **Остановка:**
    ```bash
    docker compose down
    ```
    Эта команда остановит и удалит контейнеры, но **данные базы данных в volume сохранятся**.

5.  **Полная очистка (с удалением данных БД):**
    **ВНИМАНИЕ! ЭТО УДАЛИТ ВСЕ ДАННЫЕ В БАЗЕ.** Используйте, если хотите начать с чистого листа или при проблемах с паролем/инициализацией.
    ```bash
    docker compose down -v
    ```

## Назначение первого администратора

После того как бот запущен (через Docker Compose) и база данных создана:

1.  **Найдите Telegram ID** нужного пользователя.
2.  Убедитесь, что этот пользователь **запустил бота** командой `/start` хотя бы один раз (чтобы он появился в базе данных).
3.  **Выполните команду в терминале** (не останавливая работающие контейнеры):
    ```bash
    docker compose exec bot python main.py --set-admin <USER_ID>
    ```
    Замените `<USER_ID>` на реальный ID пользователя.
4.  Проверьте логи команды (они выведутся в терминал). Вы должны увидеть сообщение об успешном назначении роли или об ошибке.

## Добавление тестовых данных (Опционально)

Если вы хотите заполнить базу данных тестовыми пользователями и заявками для проверки:

1.  **Убедитесь, что контейнеры запущены:** `docker compose ps`
2.  **(Рекомендуется)** Перед первым запуском скрипта очистите базу данных, если хотите начать с чистого листа: `docker compose down -v` и затем `docker compose up --build -d`.
3.  **Отредактируйте списки `TEST_USERS`, `CLIENT_IDS`, `ENGINEER_IDS`, `TEST_REQUESTS_DATA`** в файле `add_test_data.py`, если необходимо.
4.  **Запустите скрипт внутри контейнера бота:**
    ```bash
    docker compose exec bot python add_test_data.py
    ```
5.  Проверьте вывод скрипта на наличие ошибок.

## Использование

После запуска бота найдите его в Telegram и отправьте команду `/start`. Далее следуйте инструкциям и используйте кнопки меню в зависимости от вашей роли. Администраторы могут использовать команду `/admin` или соответствующую кнопку меню для доступа к панели управления.